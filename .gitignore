# The dataset is 89MB of parquet derived deterministically from a public download.
# `make data` rebuilds it byte-for-byte; a repo that ships it is a repo nobody clones.
data/raw/
data/processed/

.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.DS_Store
*.egg-info/
build/
dist/
